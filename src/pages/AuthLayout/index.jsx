import React from 'react';
// import PropTypes from 'prop-types';

// Auth.propTypes = {
    
// };

function Auth(props) {
    return (
        <div>
                Auth
        </div>
    );
}

export default Auth;