import React from 'react';
import PropTypes from 'prop-types';

RAM.propTypes = {
    
};

function RAM(props) {
    return (
        <div>
            RAM Page
        </div>
    );
}

export default RAM;