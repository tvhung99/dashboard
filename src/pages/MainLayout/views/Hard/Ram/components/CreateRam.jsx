import React from 'react';
import FormCreateRam from './FormCreateRam';

function CreateRam(props) {
    const handleSubmit = () =>{
        
    }
    return (
        <FormCreateRam onSubmit={handleSubmit} />
    );
}

export default CreateRam;