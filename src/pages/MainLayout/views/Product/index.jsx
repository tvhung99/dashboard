import React from 'react';
import { Outlet } from 'react-router';

Product.propTypes = {
    
};

function Product(props) {

    return (
        <>
            <Outlet />
        </>

    );
}

export default Product;